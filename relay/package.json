{
  "name": "@cantor/relay",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "check": "wrangler types --include-runtime false --check && tsc --noEmit",
    "types": "wrangler types --include-runtime false",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "@scure/base": "2.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260719.1",
    "@types/node": "26.1.1",
    "typescript": "5.9.3",
    "wrangler": "4.112.0"
  }
}
