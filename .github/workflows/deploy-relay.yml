name: deploy-relay

on:
  push:
    branches: [main]
    paths:
      - 'relay/**'
      # install.sh ships as a Worker asset, so editing it changes nothing for
      # anyone until the relay is redeployed. Leaving it out of this list is
      # what let the published installer go stale during Phase D.
      - 'node/install.sh'
      - '.github/workflows/deploy-relay.yml'
  workflow_dispatch:

# Never let two deploys race; a queued one should wait rather than be dropped,
# because the later commit is the one that must end up live.
concurrency:
  group: deploy-relay
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v5

      - name: the deploy credential is present
        env:
          TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          set -eu
          if [ -z "${TOKEN:-}" ]; then
            echo "CLOUDFLARE_API_TOKEN is not set." >&2
            echo "Add it under Settings → Secrets and variables → Actions." >&2
            exit 1
          fi

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: relay/package-lock.json

      - working-directory: relay
        run: npm ci

      # The same gates CI runs. A relay that fails its own tests should never
      # reach the domain the installer is served from.
      - working-directory: relay
        run: npx vitest run
      - working-directory: relay
        run: npm run check

      - name: deploy
        working-directory: relay
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler deploy

      # Proves the asset that actually shipped is the file in this commit,
      # which is the specific thing that silently drifted before. A fresh deploy
      # takes a moment to reach every edge, so a single immediate fetch would
      # fail intermittently and teach everyone to ignore this step.
      - name: the published installer matches this commit
        run: |
          set -eu
          for attempt in 1 2 3 4 5 6; do
            curl -fsSL https://cantor.ckadirt.xyz/install.sh -o published-install.sh || true
            if [ -s published-install.sh ] && diff -q node/install.sh published-install.sh >/dev/null; then
              echo "published install.sh matches node/install.sh (attempt $attempt)"
              exit 0
            fi
            echo "not live yet; retrying in 10s (attempt $attempt)"
            sleep 10
          done
          echo "the published installer never matched this commit:" >&2
          diff -u node/install.sh published-install.sh >&2 || true
          exit 1
